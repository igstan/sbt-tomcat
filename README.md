## Tasks

 - `tomcat:start`
 - `tomcat:stop`
 - `tomcat:reload`

## Settings

 - `baseDirectory`; default `target / "tomcat"`
 - `webappDirectory`; default `baseDirectory / "src/main/webapp"`
 - `contextPath`, default `"/"`
 - `port`, default `8080`
